<%= form_for @track, :url => tracks_path, :html => { :multipart => true } do |f| %>
  <% if @track.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@track.errors.count, "error") %> prohibited this track from being saved:</h2>

      <ul>
      <% @track.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.file_field :song %>
  </div>
  <div class="field">
    <%= f.label :album %>
    <%= select_tag "track[album_id]", options_from_collection_for_select(Album.all,"id","title") %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
